% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getUserMediaAPI.R
\name{getUserMediaAPI}
\alias{getUserMediaAPI}
\title{Get a User's Media Tweets via the X API (experimental)}
\usage{
getUserMediaAPI(username = "NASA", n_tweets = 40, dir = getwd(), save = TRUE)
}
\arguments{
\item{username}{Nombre de usuarix (sin @). Por defecto "NASA".}

\item{n_tweets}{Numero maximo de tweets a recuperar. Por defecto 40.}

\item{dir}{Directorio de destino del RDS. Por defecto el de trabajo.}

\item{save}{Logico. Si TRUE (por defecto) guarda el resultado en un RDS.}
}
\value{
Un tibble con un tweet por fila (mismas columnas que getUserTweetsAPI).
}
\description{
\if{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental" target="_blank">}}\if{html}{\out{<img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]">}}\if{html}{\out{</a>}}

Recupera los tweets con media (fotos/videos) de unx usuarix consultando la
\strong{API GraphQL interna de X} (UserMedia). Devuelve datos estructurados del
JSON. Requiere una sesion importada con \code{\link[=importSessionX]{importSessionX()}}.
}
\examples{
\dontrun{
importSessionX(auth_token = "...", ct0 = "...")
getUserMediaAPI("NASA", n_tweets = 100)
}
}
