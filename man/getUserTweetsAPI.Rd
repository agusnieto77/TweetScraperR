% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getUserTweetsAPI.R
\name{getUserTweetsAPI}
\alias{getUserTweetsAPI}
\title{Get Tweets from a User Timeline via the X API (experimental)}
\usage{
getUserTweetsAPI(username = "NASA", n_tweets = 40, dir = getwd(), save = TRUE)
}
\arguments{
\item{username}{Nombre de usuarix (sin @). Por defecto "NASA".}

\item{n_tweets}{Numero maximo de tweets a recuperar. Por defecto 40.}

\item{dir}{Directorio de destino del RDS. Por defecto el de trabajo.}

\item{save}{Logico. Si TRUE (por defecto) guarda el resultado en un RDS.}
}
\value{
Un tibble con un tweet por fila y columnas: fecha, user, texto,
idioma, respuestas, retweets, citas, megustas, views, hashtags (lista),
menciones (lista), urls_externas (lista), media (lista de URLs),
media_tipo (lista: photo/video/animated_gif), es_retweet, es_cita,
tweet_citado_id, conversation_id, url y tweet_id.
}
\description{
\if{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental" target="_blank">}}\if{html}{\out{<img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]">}}\if{html}{\out{</a>}}

Recupera los tweets del timeline de unx usuarix consultando la \strong{API GraphQL
interna de X} (en lugar de scrapear HTML). Devuelve datos estructurados
directamente del JSON: texto completo (sin truncar), fecha exacta, y metricas
(respuestas, retweets, citas, me gusta, vistas). Requiere una sesion
importada con \code{\link[=importSessionX]{importSessionX()}}.
}
\examples{
\dontrun{
importSessionX(auth_token = "...", ct0 = "...")
tw <- getUserTweetsAPI("NASA", n_tweets = 100)
}
}
