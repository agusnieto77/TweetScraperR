% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getTweetsTimelinesAPI.R
\name{getTweetsTimelinesAPI}
\alias{getTweetsTimelinesAPI}
\title{Get the Combined Timeline of Several Users via the X API (experimental)}
\usage{
getTweetsTimelinesAPI(usernames, n_tweets = 40, dir = getwd(), save = TRUE)
}
\arguments{
\item{usernames}{Vector de nombres de usuarix (acepta "NASA", "@NASA" o la
URL completa "https://x.com/NASA").}

\item{n_tweets}{Numero maximo de tweets a recuperar \strong{por cuenta}. Por
defecto 40.}

\item{dir}{Directorio de destino del RDS. Por defecto el de trabajo.}

\item{save}{Logico. Si TRUE (por defecto) guarda el resultado en un RDS.}
}
\value{
Un tibble con un tweet por fila (mismas columnas que
\code{\link[=getUserTweetsAPI]{getUserTweetsAPI()}}), combinado y ordenado por fecha.
}
\description{
\if{html}{\out{<a href="https://lifecycle.r-lib.org/articles/stages.html#experimental" target="_blank">}}\if{html}{\out{<img src="https://lifecycle.r-lib.org/articles/figures/lifecycle-experimental.svg" alt="[Experimental]">}}\if{html}{\out{</a>}}

Recupera y combina los timelines de varias cuentas consultando la \strong{API
GraphQL interna de X}, en una sola sesion de navegador (batch). Util para
monitorear un conjunto curado de cuentas (lo que harias con una Lista de X,
pero sin necesitar un list_id). Devuelve un unico tibble ordenado por fecha
(mas reciente primero) y deduplicado. Requiere una sesion importada con
\code{\link[=importSessionX]{importSessionX()}}.
}
\examples{
\dontrun{
importSessionX(auth_token = "...", ct0 = "...")
getTweetsTimelinesAPI(c("elravignani", "NucleoIdaes", "BNMMArgentina"), n_tweets = 100)
}
}
